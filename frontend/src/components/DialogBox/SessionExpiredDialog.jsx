import './DialogBox.css';

const SessionExpiredDialog = () => {
  return ( 
    <div className="dialog expired">Your session has expired. Please log in again.</div>
   );
}
 
export default SessionExpiredDialog;